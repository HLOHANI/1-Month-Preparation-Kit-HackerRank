void miniMaxSum(vector<int> arr) {
    int n = arr.size();
    sort(arr.begin(),arr.end());
    long int min=0,max=0;
    for(int i=0;i<n-1;i++)
    {
        min+=arr[i];
    }
    for(int i=1;i<5;i++)
    {
        max+=arr[i];
    }
    cout<<min<<" "<<max<<endl;
    
}
